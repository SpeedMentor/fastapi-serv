[pytest]
markers =
    database: marks tests that require database connection
    integration: marks integration tests
    unit: marks unit tests
    asyncio: marks tests that use asyncio
addopts = -v -m "not database" --disable-warnings
asyncio_mode = auto 